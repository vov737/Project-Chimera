# 🌊 WindBrowser (Project "Chimera"): Браузер на Python

**WindBrowser** — это образовательный проект, демонстрирующий базовую архитектуру и конвейер рендеринга современного веб-браузера. Движок полностью реализован на Python и включает в себя парсер, движок стилей (CSS1), расчет макета (Box Model), отрисовку (с поддержкой OpenGL/FLTK) и среду выполнения JavaScript (Duktape).

---

## 🇷🇺 Русский

### О проекте

Проект **WindBrowser** был создан с целью изучения и пошаговой реализации критических компонентов веб-браузера. Мы достигли полной поддержки **CSS1** и базовой интерактивности.

### Ключевые Возможности

* **Полный Конвейер Рендеринга:** Парсинг HTML (DOM) → Парсинг CSS (CSSOM) → Расчет стилей (Каскад).
* **CSS Box Model:** Полная поддержка `margin`, `padding` и `border` по стандарту CSS1.
* **Движок Layout:** Реализация "нормального потока" для блочных элементов.
* **Отрисовка (Painting):** Использование `pyFLTK` для UI и `pyOpenGL` в качестве бэкенда для рендеринга примитивов.
* **Интерактивность:** Интеграция Duktape для выполнения JavaScript, включая базовый DOM API (`document.getElementById`, `element.style.prop`).
* **Навигация:** Адресная строка, история (кнопки Назад/Вперед) и поддержка прокрутки.

### Требования

Для запуска WindBrowser необходимы следующие библиотеки:

```bash
pip install pyfltk lxml requests pyduktape pyopengl
